# Runtime folder used by both the external MCP server and the Abaqus-side plugin.
# Defaults to %USERPROFILE%\.abaqus-mcp on Windows if unset.
ABAQUS_MCP_HOME=C:\path\to\text-to-cae\MCP\Abaqus

# Optional: set this if your MCP client does not use the venv Python directly.
PYTHONPATH=
