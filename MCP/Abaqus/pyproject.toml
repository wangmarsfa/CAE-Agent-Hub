[project]
name = "abaqus-mcp"
version = "4.0.0"
description = "MCP server and Abaqus/CAE file-IPC bridge"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0,<9"]

[tool.uv]
package = false
